package efme.examples.ex03 
{
	/**
	 * @private
	 * 
	 * Enumeration of my example application's actions.
	 * 
	 * The <code>Keyboard</code> class accepts any int as an action argument,
	 * but for code readability and to reduce the chance of making an error,
	 * creating an eneration like this is really recommended.
	 * 
	 * @see emfe.examples.Ex03_Input
	 */
	public class UserActions
	{
		public static const MOVE_RIGHT:String = "MOVE_RIGHT";
		public static const MOVE_LEFT:String = "MOVE_RIGHT"; // TODO: Change to MOVE_LEFT
		public static const SHOOT:String = "SHOOT";
	}

}